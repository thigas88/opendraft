[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "opendraft"
version = "1.7.4"
description = "Open-source AI research draft generator with 19 specialized agents and verified citations"
readme = "README.md"
license = {text = "MIT"}
authors = [
    {name = "Federico De Ponte", email = "federico@opendraft.xyz"}
]
keywords = [
    "academic writing",
    "thesis generator",
    "research paper",
    "AI writing",
    "citation verification",
    "literature review",
    "multi-agent AI",
    "ChatGPT alternative",
    "dissertation",
    "open source"
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "Intended Audience :: Education",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Education",
    "Topic :: Text Processing :: Linguistic",
]
requires-python = ">=3.10"

[project.urls]
Homepage = "https://openpaper.dev?utm_source=pypi&utm_medium=package&utm_campaign=opendraft"
Repository = "https://github.com/federicodeponte/opendraft"
Documentation = "https://github.com/federicodeponte/opendraft#readme"
Issues = "https://github.com/federicodeponte/opendraft/issues"
Changelog = "https://github.com/federicodeponte/opendraft/blob/master/CHANGELOG.md"

[project.optional-dependencies]
pdf = ["PyPDF2", "pdfplumber"]
dev = ["pytest", "black", "ruff"]
figures = ["matplotlib>=3.7.0", "seaborn>=0.12.0"]
annotations = ["PyMuPDF>=1.23.0"]
paper-machine = ["matplotlib>=3.7.0", "seaborn>=0.12.0", "PyMuPDF>=1.23.0"]

[project.scripts]
opendraft = "opendraft.cli:main"
draft-ai = "opendraft.cli:main"
opendraft-tldr = "tldr:main"
opendraft-digest = "digest:main"

[tool.setuptools.packages.find]
where = ["engine"]
include = ["opendraft*", "utils*", "concurrency*", "phases*", "prompts*"]

[tool.setuptools]
package-dir = {"" = "engine"}
py-modules = ["config", "draft_generator", "tldr", "digest"]
include-package-data = true

[tool.setuptools.package-data]
"prompts" = ["*.md", "*/*.md", "**/*.md"]
"opendraft" = ["prompts/*.md", "prompts/**/*.md"]
"*" = ["*.md"]
